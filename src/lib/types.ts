export interface DiffItem {
    id: string;
    description: string;
    diff: string;
    url: string;
  }
  
  export interface Note {
    id: string;
    developerNote: string;
    marketingNote: string;
  }